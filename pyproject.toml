[project]
name = "wechat-windows-versions"
version = "0.1.0"
description = "Automated Archive of WeChat for Windows "
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
  "httpx>=0.28.1",
  "python-dateutil>=2.9.0.post0",
]
